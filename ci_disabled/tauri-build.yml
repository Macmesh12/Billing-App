name: Build Tauri Cross-Platform (disabled backup)

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is a disabled backup of .github/workflows/tauri-build.yml"

# Original file moved here as backup. Restore to .github/workflows/ if needed.
